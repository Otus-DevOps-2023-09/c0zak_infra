---
- name: Install Ruby
  hosts: all
  become: true
  roles:
    - app
  # tasks:

  #   - name: Install Ruby
  #     apt:
  #       name: "{{ item  }}"
  #       state: present
  #     with_items:
  #       - ruby-full
  #       - ruby-bundler
  #       - build-essential
